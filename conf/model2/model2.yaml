name: model2

target:
  _target_: scripts.network.models.flow4D_ori_multi.Flow4D_forflow
  voxel_size: ${voxel_size}
  point_cloud_range: ${point_cloud_range}
  num_frames: ${num_frames}

val_monitor: val/Dynamic/Mean